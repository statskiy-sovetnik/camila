# public/media

Puzzle 6 plays a ~15 second The Neighbourhood clip, which is not part of the design handoff.

Drop it here as `neighbourhood-clip.mp4` — `src/data/puzzles.ts` points at `/media/neighbourhood-clip.mp4`.

Files in `public/` are copied to the build root as-is and are not hashed or processed by Vite.
